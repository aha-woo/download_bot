# Telegram User API Configuration (必需)
# 从 https://my.telegram.org/apps 获取
API_ID=your_api_id_here
API_HASH=your_api_hash_here
PHONE_NUMBER=+1234567890

# Channel Configuration
SOURCE_CHANNEL_ID=@your_source_channel
TARGET_CHANNEL_ID=@your_target_channel

# 多源频道配置 (可选) - 用逗号分隔多个频道
# SOURCE_CHANNELS=@channel1,@channel2,@channel3,-1001234567890

# Session Settings (会话文件设置)
SESSION_NAME=telegram_session
SESSION_PATH=./

# Download Settings
DOWNLOAD_PATH=./downloads
MAX_FILE_SIZE=2GB

# 随机延迟设置 (防止被检测为机器人)
RANDOM_DELAY_MIN=2
RANDOM_DELAY_MAX=15
BATCH_DELAY_MIN=30
BATCH_DELAY_MAX=120

# 消息队列设置 (延迟发送系统)
QUEUE_ENABLED=false                 # 是否启用队列模式 (true/false)
MIN_SEND_DELAY=300                  # 最小发送延迟 (秒) - 5分钟
MAX_SEND_DELAY=7200                 # 最大发送延迟 (秒) - 2小时
QUEUE_CHECK_INTERVAL=30             # 队列检查间隔 (秒)
MAX_QUEUE_SIZE=100                  # 最大队列大小 (条消息)

# 分批发送设置
BATCH_SEND_ENABLED=false            # 是否启用分批发送模式 (true/false)
BATCH_SIZE=5                        # 每批消息数量
BATCH_INTERVAL=1800                 # 批次间隔 (秒) - 30分钟

# 队列持久化设置
QUEUE_SAVE_PATH=./queue_data.json   # 队列数据保存路径
AUTO_SAVE_QUEUE=true                # 自动保存队列状态 (true/false)

# 代理设置 (重要! VPS环境强烈建议启用)
PROXY_ENABLED=false                 # 是否启用代理 (true/false)
PROXY_TYPE=socks5                   # 代理类型: socks5, socks4, http
PROXY_HOST=                         # 代理服务器地址
PROXY_PORT=1080                     # 代理端口
PROXY_USERNAME=                     # 代理用户名 (可选)
PROXY_PASSWORD=                     # 代理密码 (可选)
PROXY_RDNS=true                     # 远程DNS解析 (true/false)

# 代理轮换设置 (高级功能)
PROXY_ROTATION_ENABLED=false        # 是否启用代理轮换 (true/false)
PROXY_ROTATION_INTERVAL=3600        # 代理轮换间隔 (秒) - 1小时
PROXY_LIST_FILE=./proxy_list.txt    # 代理列表文件路径

# 代理测试设置
PROXY_TEST_ENABLED=true             # 是否测试代理连通性 (true/false)
PROXY_TEST_TIMEOUT=10               # 代理测试超时时间 (秒)

# 重要说明:
# 1. API_ID 和 API_HASH 必须从 https://my.telegram.org/apps 获取
# 2. PHONE_NUMBER 必须是国际格式 (+国家代码+号码)
# 3. 首次运行时需要输入验证码
# 4. User API 支持2GB大文件下载，无20MB限制
# 5. 队列模式：消息下载后加入队列，延迟随机时间发送
# 6. 立即模式：消息下载后立即发送 (2-15秒延迟)
# 7. 使用 /mode 命令可在运行时切换模式
# 8. VPS环境强烈建议启用代理：PROXY_ENABLED=true
# 9. 住宅代理安全性最高，数据中心代理风险较大
# 10. 代理轮换功能可进一步降低检测风险